app = "api-upload"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "java -jar target/api-upload-0.0.1-SNAPSHOT.jar"

[env]
  PORT = "8080"
  
[[services]]
  http_checks = []
  internal_port = 8080
  protocol = "tcp"

[metrics]
  port = 9091
  path = "/metrics"
